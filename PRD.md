# 诗词 App · 开发需求文档 v1.0

> 产品代号：**诗笺**（Poetry Note）
> 设计理念：**月光白 · 极简留白 · 每日一首**
> 技术栈：Kotlin + Jetpack Compose + Material3
> 数据源：诗泉 Chinese Poetry API（REST + GraphQL）

---

## 1. 项目概述

一款纯粹的个人诗词阅读 Android App。不登录、不联网验证、无广告、无社区。
核心体验是「打开就有一首今日之诗，上下滑动随手翻」，像一个只属于你和诗词的安静角落。

### 目标用户
- 老板 & 老板的对象，仅此两人
- 不需要考虑多用户、权限、国际化（仅中文）

### 核心原则
| 原则 | 描述 |
|------|------|
| **离线可用** | 已读/收藏诗词本地缓存，无网可复读 |
| **极速启动** | 开屏即诗，不需 loading |
| **字体自由** | 默认仿宋，可切楷/宋/隶/自定义 |
| **不打扰** | 无推送、无广告、无开屏 |
| **数据归属** | 全本地，不上传任何数据 |

---

## 2. 技术方案

| 层级 | 选型 | 说明 |
|------|------|------|
| 语言 | Kotlin 1.9+ | |
| UI | Jetpack Compose + Material3 | 声明式 UI，动画原生支持 |
| 架构 | MVVM + Repository | |
| 网络 | Retrofit + OkHttp | 调用诗泉 API |
| 本地存储 | Room | 收藏/历史/缓存 |
| 图片加载 | Coil | 对古画等大图友好 |
| 字体加载 | Compose 原生 `FontFamily` + TTF 动态加载 | |
| 配色 | Material3 动态主题 | 预设月光白 + 墨色夜读 |

---

## 3. 配色体系

### 月光白（日间模式）

| 用途 | 色值 | 描述 |
|------|------|------|
| 背景主色 | `#FBFBF9` | 暖白，像月光洒在宣纸上 |
| 卡片/面板 | `#F5F3F0` | 极浅米灰，层次感 |
| 主文字 | `#2C2C2C` | 深灰，不刺眼 |
| 次文字 | `#99958F` | 作者/日期/标签 |
| 强调色 | `#C4A97D` | 暖金，收藏/选中/图标 |
| 分割线 | `#E8E5E0` | 极淡，几乎看不见 |
| 底部栏 | `#FFFFFF` + 顶部 2dp 阴影 | 悬浮感 |
| 错误/无网络 | `#C49D9D` | 极淡的干燥玫瑰色 |

### 墨色夜读（深色模式）

| 用途 | 色值 |
|------|------|
| 背景 | `#1A1A1A` |
| 卡片 | `#242424` |
| 主文字 | `#E0DDD5` |
| 次文字 | `#88847C` |
| 强调色 | `#B8956A` |
| 分割线 | `#2E2E2E` |

---

## 4. 页面设计

### 4.1 底部导航栏（全局）

```
┌──────────────────────────────────────────────┐
│                                              │
│               内容区域                         │
│                                              │
├──────────────────────────────────────────────┤
│  每日        文库        发现        我的     │
│  ──                                          │
│  （选中态：图标+文字变为强调色#C4A97D）         │
│  图标下方 2dp 圆点指示器                       │
└──────────────────────────────────────────────┘
```

- 切换平滑无闪烁
- 导航栏背景 `#FFFFFF`，带 2dp 顶部阴影实现悬浮效果
- 深色模式背景 `#1E1E1E`，阴影变为微弱光晕

---

### 4.2 每日页（首页 · 默认 Tab）

#### 4.2.1 布局结构

```
┌──────────────────────────────────────────────┐
│  ●  ●  ○  ○  ○ （页面指示器，最多5点）         │  ← 顶部 6dp
│                                              │
│              [渐变色背景区域]                   │
│          （半透明，从诗名上方渐入）              │
│                                              │
│              静 夜 思                          │  ← 诗名，28sp，仿宋 Bold
│                                              │
│              李 白                            │  ← 作者，15sp，#99958F
│                                              │
│           床前明月光  疑是地上霜                  │
│                                              │  ← 正文，18sp，仿宋 Regular
│           举头望明月  低头思故乡                  │  ← 行距 1.8，字距 0.3sp
│                                              │
│            ── 唐 · 五言绝句 ──                  │  ← 12sp，次文字色
│                                              │
│          ♡ 收藏       ↻ 换一首                  │  ← 图标+文字，底部操作栏
│                                              │
│             2026.07.26  周日                    │  ← 12sp，日期落款
│                                              │
│  ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─  │
│          ↑ 上滑切下一首 · 下滑切上一首 ↑         │
│  ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─  │
│      （下一首预览：诗名露一行 + 渐变透出）       │
└──────────────────────────────────────────────┘
```

#### 4.2.2 交互细节

| 交互 | 行为 |
|------|------|
| **上滑** | 当前诗平滑上移淡出（0->1 偏移，alpha 1->0），下一首从底部滑入 |
| **下滑** | 反向动画，回到上一首 |
| **点击收藏** | 心形图标填充动画（即填充成实心），本地存 Room |
| **点击换一首** | 触发智能随机算法（见 4.2.4），卡片翻转动画切换 |
| **长按正文** | 选中文字，支持复制 |
| **点击作者** | 跳转到该作者在文库的作品列表 |

#### 4.2.3 推荐算法（核心）

```
每日推荐逻辑（首次打开 App / 每天首次进入每日页）：

1. 获取当前时间（小时 + 月份 + 节气）
2. 匹配时令：
   a. 检测是否有今日节气（春分/清明/中秋）→ 有则返回对应诗词
   b. 没有节气 → 检测当前时段：
      - 05:00-11:59 → 晨诗（写清晨/朝阳/春日的诗）
      - 12:00-17:59 → 闲适诗（写午后/闲居/山水的诗）
      - 18:00-04:59 → 夜诗（写月夜/秋夜/思乡的诗）
3. 如果该时段/节气有缓存（今日已推过）→ 从排序队列取下一首
4. 如果没有缓存 → 调诗泉 API，按时段/节气过滤 random
5. 如果 API 失败或没网 → 从本地缓存取一首未读的
6. 展示诗词，同时将内容 + 渐变色参数写入 Room 缓存
```

#### 4.2.4 换一首逻辑（智能随机）

```
1. 检查当前诗是否满足「触发偏好」条件：
   - 被收藏过 OR 在当前页停留超过 10 秒
2. 满足 → 偏同类推荐（60% 概率）：
   - 取当前诗的作者，调 API `/random?author=李白`
   - 如果失败则降级为纯随机
3. 不满足 → 纯随机（100% 概率）：
   - 调 API `/poems/random`
4. 如果新诗和当前诗是同一首 → 重新随机
5. 缓存新诗内容到 Room
```

#### 4.2.5 背景图生成

```
哈希渐变生成算法：

1. 取诗词正文字符串 → SHA256 → hex 串
2. 取前 6 位 → 转十进制 → 色相 H（0-360）
3. 取 7-12 位 → 转十进制 → 按比例映射到饱和度 S（20-50）
4. 取 13-18 位 → 转十进制 → 按比例映射到明度 L（60-85）
5. 生成径向渐变：
   - 中心点：60% x, 40% y
   - 起始色：HSL(H, S, L) @ 60% 透明度
   - 终止色：HSL(H+30, S-10, L+10) @ 10% 透明度
6. 底部 30% 区域线性渐变过渡到纯白（#FBFBF9）
   - 实现图片与卡片底部自然融合

画库匹配：

1. 内置 200-500 张国风图，每张标注：[主题, 季节, 色调, 情感]
2. 当前诗按 [体裁, 作者朝代, 内容关键词] 提取特征
3. 计算余弦相似度，得分 > 0.7 则优先展示配图
4. 否则降级到哈希渐变
```

---

### 4.3 文库页

#### 4.3.1 默认状态

```
┌──────────────────────────────────────────────┐
│  文库                            🔍  【▦】    │  ← 搜索图标 + 视图切换
│                                              │
│  ┌──────────────────────────────────────────┐│
│  │  全部  ▾                                 ││
│  │  全部  唐  宋  元  明  清  先秦  其他    ││  ← 横向滚动标签
│  └──────────────────────────────────────────┘│
│                                              │
│  （列表模式）                                 │
│  ┌──────────────────────────────────────────┐│
│  │  静夜思                        李白  唐 ▸││
│  │  床前明月光，疑是地上霜...                ││
│  ├──────────────────────────────────────────┤│
│  │  将进酒                        李白  唐 ▸││
│  │  君不见黄河之水天上来...                  ││
│  ├──────────────────────────────────────────┤│
│  │  声声慢                      李清照  宋 ▸││
│  │  寻寻觅觅，冷冷清清...                    ││
│  └──────────────────────────────────────────┘│
│                                              │
│  共 2,358 首                    加载更多      │
└──────────────────────────────────────────────┘
```

#### 4.3.2 网格模式

```
┌──────────────────────────────────────────────┐
│  文库                            🔍  【≡】    │  ← 切换回列表
│                                              │
│  ┌──────┐  ┌──────┐  ┌──────┐               │
│  │静夜思 │  │将进酒 │  │声声慢 │               │
│  │ 李白  │  │ 李白  │  │李清照│               │
│  │ 唐 ▸  │  │ 唐 ▸  │  │ 宋 ▸ │               │
│  └──────┘  └──────┘  └──────┘               │
│  ┌──────┐  ┌──────┐  ┌──────┐               │
│  │春晓   │  │江雪   │  │望月   │               │
│  │孟浩然 │  │柳宗元 │  │杜甫  │               │
│  │ 唐 ▸  │  │ 唐 ▸  │  │ 唐 ▸ │               │
│  └──────┘  └──────┘  └──────┘               │
│                                              │
│  每项：圆角 16dp 卡片，浅底 #F5F3F0          │
│  诗名 18sp，作者 13sp，朝代标签 11sp          │
└──────────────────────────────────────────────┘
```

#### 4.3.3 搜索

```
┌──────────────────────────────────────────────┐
│  ← 取消                 🔍 搜索               │
│                                              │
│  ┌──────────────────────────────────────────┐│
│  │  🔍 输入诗名/作者/诗句...               ││
│  └──────────────────────────────────────────┘│
│                                              │
│  搜索类型：全部  ·  标题  ·  作者  ·  内容    │
│              ───（选中态下划线）              │
│                                              │
│  ┌ 搜索结果 ───────────────────────────────┐│
│  │  静夜思                        李白  唐 ▸││
│  │  ...床前明<月>光...（匹配字高亮暖金）    ││
│  ├──────────────────────────────────────────┤│
│  │  月下独酌                      李白  唐 ▸││
│  │  ...举杯邀明<月>...                     ││
│  └──────────────────────────────────────────┘│
│                                              │
│  搜索类型为「内容」时，匹配行用暖金色高亮      │
│  搜索请求调 诗泉 API /poems/search            │
│  有 300ms 防抖，避免频繁请求                  │
└──────────────────────────────────────────────┘
```

---

### 4.4 详情页（从文库/发现/收藏进入）

```
┌──────────────────────────────────────────────┐
│  ← 返回                    ♡ 收藏   ⋮ 更多   │
│                                              │
│            [渐变色背景区域（轻量版）]           │
│                                              │
│              静 夜 思                          │
│              李 白                            │
│                                              │
│           床前明月光  疑是地上霜                  │
│                                              │
│           举头望明月  低头思故乡                  │
│                                              │
│            ── 唐 · 五言绝句 ──                  │
│                                              │
│  ┌─ 注释 ─────────────────────────────────┐  │
│  │  📖  静夜思：安静的夜晚产生的思绪            │  │
│  │       疑是：好像是，以为是的。              │  │
│  └──────────────────────────────────────────┘  │
│                                                │
│  ┌─ 赏析 ─────────────────────────────────┐  │
│  │  🔍  此诗以明白如话的语言，写游子思乡...    │  │
│  │       前两句写景，后两句抒情...            │  │
│  └──────────────────────────────────────────┘  │
│                                                │
│  ┌─ 相似推荐 ─────────────────────────────┐  │
│  │  《月下独酌》李白   →                    │  │
│  │  《山居秋暝》王维   →                    │  │
│  │  《枫桥夜泊》张继   →                    │  │
│  └──────────────────────────────────────────┘  │
│                                                │
│  上下滚动阅读整页                               │
└────────────────────────────────────────────────┘
```

- 注释/赏析/相似推荐为可折叠面板，默认收起
- 相似推荐：同作者 + 同体裁混合，调诗泉 API 过滤

---

### 4.5 发现页

```
┌──────────────────────────────────────────────┐
│  发现                                         │
│                                              │
│  🔥 热门排行（本周）                          │
│  ┌──────────────────────────────────────────┐│
│  │  1.  静夜思                    李白     ││
│  │      ⭐ 98.2k 次收藏                     ││
│  │  2.  将进酒                    李白     ││
│  │      ⭐ 95.1k 次收藏                     ││
│  │  3.  水调歌头                  苏轼     ││
│  │      ⭐ 92.7k 次收藏                     ││
│  │  4.  春晓                      孟浩然   ││
│  │  5.  江雪                      柳宗元   ││
│  │  6.  望月怀远                  张九龄   ││
│  └──────────────────────────────────────────┘│
│                                              │
│  🎲 随机一首                                  │
│  ┌──────────────────────────────────────────┐│
│  │          ┌──────────────┐               ││
│  │          │  翻一张卡     │               ││
│  │          │  🎴          │               ││
│  │          │  命运会给你   │               ││
│  │          │  哪首诗？     │               ││
│  │          └──────────────┘               ││
│  │                                          ││
│  │  点击卡片 → 翻转动画 → 显示诗名和首句     ││
│  │  再点击 → 跳转详情页                      ││
│  └──────────────────────────────────────────┘│
│                                              │
│  📅 本周回顾                                  │
│  ┌──────────────────────────────────────────┐│
│  │  周一  静夜思                   已读 ✓   ││
│  │  周二  将进酒                   已读 ✓   ││
│  │  周三  水调歌头                 未读 ○   ││
│  │  周四  春晓                     已读 ✓   ││
│  │  周五  江雪                     未读 ○   ││
│  │  周六  望月怀远                 已读 ✓   ││
│  │  今日  月下独酌                  ← 现在   ││
│  │  → 点击任意一天回到当日推送               ││
│  └──────────────────────────────────────────┘│
└──────────────────────────────────────────────┘
```

- 🔥 热门排行：取本地所有用户（你和对象）的收藏数据汇总，每周一 00:00 重置
- 首次安装：内置 20 首种子数据作为初始热门榜
- 🎲 随机一首：调用诗泉 API `/poems/random`，翻转卡片动效

---

### 4.6 我的页

```
┌──────────────────────────────────────────────┐
│  我的                                         │
│                                              │
│  ┌──────────────────────────────────────────┐│
│  │  📖 我的收藏                     (12)    ││
│  │  ──────────────────────────────────────  ││
│  │  静夜思 · 李白                    2026.07││
│  │  将进酒 · 李白                    2026.07││
│  │  声声慢 · 李清照                  2026.07││
│  │  ...查看更多 ▸                           ││
│  └──────────────────────────────────────────┘│
│                                              │
│  ┌──────────────────────────────────────────┐│
│  │  🕐 浏览历史                     (38)    ││
│  │  ──────────────────────────────────────  ││
│  │  月下独酌 · 李白                  今天   ││
│  │  春晓 · 孟浩然                    昨天   ││
│  │  ...查看更多 ▸                           ││
│  └──────────────────────────────────────────┘│
│                                              │
│  ┌─ 设置 ─────────────────────────────────┐  │
│  │  🔤 字体                    仿宋 ▸     │  │
│  │  🔠 字号                  标准 ▸      │  │
│  │  🌙 深色模式               关闭 ▸     │  │
│  │  🖼 每日配图               开启 ▸     │  │
│  │  📊 数据统计                          │  │
│  │    已读 38 首 · 收藏 12 首             │  │
│  └──────────────────────────────────────────┘│
│                                              │
│  关于 · 诗泉 API · v1.0                      │
└──────────────────────────────────────────────┘
```

#### 4.6.1 字体设置面板

```
┌──────────────────────────────────────────────┐
│  ← 返回                          字体         │
│                                              │
│  ✓ 仿宋（默认）                               │
│    春风又绿江南岸  明月何时照我还             │
│                                              │
│  ○ 楷体                                      │
│    春风又绿江南岸  明月何时照我还             │
│                                              │
│  ○ 宋体（衬线）                              │
│    春风又绿江南岸  明月何时照我还             │
│                                              │
│  ○ 隶书                                      │
│    春风又绿江南岸  明月何时照我还             │
│                                              │
│  ○ 系统默认                                  │
│    春风又绿江南岸  明月何时照我还             │
│                                              │
│  ───────────────────────────────────────       │
│  + 添加本地字体                               │
│    打开文件选择器 → 选择 ttf/otf → 自动加载    │
│                                              │
│  选中后全 App 实时切换，所有诗词页即刻生效      │
└──────────────────────────────────────────────┘
```

#### 4.6.2 字号设置面板

| 档位 | 诗名字号 | 正文字号 | 次文字 |
|------|---------|---------|-------|
| 小 | 24sp | 16sp | 11sp |
| 标准（默认） | 28sp | 18sp | 12sp |
| 大 | 32sp | 20sp | 13sp |
| 超大 | 36sp | 22sp | 14sp |

#### 4.6.3 深色模式设置

```
可选：关闭 / 跟随系统 / 始终开启
切换时：渐变动画过渡（1s），背景色从暖白流到墨色
```

#### 4.6.4 每日配图设置

```
开启（默认）：有图画库图，无图用哈希渐变
关闭：强制纯文字，不展示任何配图/渐变
```

---

## 5. 数据模型

### 5.1 Room Entity

```kotlin
@Entity(tableName = "poems")
data class PoemEntity(
    @PrimaryKey val id: Long = 0,           // 诗泉API的诗词ID
    val title: String,
    val author: String,
    val dynasty: String,
    val content: String,                     // 全文，含换行
    val type: String,                        // 五言绝句/七言律诗/词/曲...
    val notes: String? = null,              // 注释
    val appreciation: String? = null,        // 赏析
    val lang: String = "zh-Hans",            // zh-Hans / zh-Hant
    val hashGradient: String,                // 缓存的HSL色值，如 "220,35,72"
    val cachedAt: Long = System.currentTimeMillis(),
)

@Entity(tableName = "favorites")
data class FavoriteEntity(
    @PrimaryKey val poemId: Long,
    val createdAt: Long = System.currentTimeMillis(),
)

@Entity(tableName = "read_history")
data class ReadHistoryEntity(
    @PrimaryKey(autoGenerate = true) val id: Long = 0,
    val poemId: Long,
    val readAt: Long = System.currentTimeMillis(),
)
```

### 5.2 画库图片索引（本地 JSON）

> 图片来源：**公版古画**（宋画/明画/清画，Wikimedia Commons 等公有领域国画）
> 精选 200-500 张，按主题/季节/情感分类
> 不做 AI 生成图，不侵版权

```json
{
  "images": [
    {
      "id": "mountain_001",
      "filename": "mountain_001.webp",
      "tags": ["山水", "秋", "冷色调", "离别"],
      "dynasty": ["唐"],
      "mood": ["寂寥", "深远"]
    }
  ]
}
```

### 5.3 每日推荐记录

```kotlin
@Entity(tableName = "daily_recommendations")
data class DailyRecEntity(
    @PrimaryKey val date: String,            // "2026-07-26"
    val poemId: Long,
    val slot: String,                        // "morning" / "afternoon" / "night"
    val recommendedAt: Long,
)
```

---

## 6. API 对接方案

### 6.1 诗泉 API 端点

| 功能 | 端点 | 参数 |
|------|------|------|
| 随机诗词 | `GET /api/v1/poems/random` | `?author=&type=&dynasty=&char=` |
| 搜索 | `GET /api/v1/poems/search` | `?q=&type=all\|title\|content\|author` |
| 作者列表 | `GET /api/v1/authors` | `?page=&page_size=` |
| 作者详情 | `GET /api/v1/authors/{id}` | |
| 朝代列表 | `GET /api/v1/dynasties` | |
| 诗词体裁 | `GET /api/v1/types` | |
| 统计 | `GET /api/v1/stats` | |

### 6.2 默认 Base URL

```
https://poetry.palemoky.com
```

> 可在「我的 → 设置」中自定义 API 地址（为将来自建预留）

### 6.3 缓存策略

| 场景 | 策略 |
|------|------|
| 每日推荐 | 获取时缓存到 Room，保存7天自动清理 |
| 浏览诗词 | 每次进入详情页/每日页，自动缓存全文到 `poems` 表 |
| 搜索 | 搜索结果不缓存，每次走网络 |
| 收藏 | 存 `favorites` 表，永远不会动 |
| 图片 | Coil 默认磁盘缓存 |

---

## 7. 页面路由

```
NavHost(startDestination = "daily") {
    composable("daily")          → 每日页（首页）
    composable("library")        → 文库页（含搜索）
    composable("discover")       → 发现页
    composable("profile")        → 我的页
    composable("poem/{id}")      → 详情页
    composable("settings/font")  → 字体设置
    composable("settings/font/add") → 添加本地字体（文件选择器）
    composable("settings/theme") → 深色模式设置
    composable("favorites")      → 收藏列表（在我的页内嵌套）
    composable("history")        → 浏览历史（在我的页内嵌套）
}
```

---

## 8. 动效清单

| 场景 | 动效 | 实现 |
|------|------|------|
| 每日页上下滑动 | 垂直滑入滑出 + alpha 渐变 | Compose `AnimatedVisibility` + `verticalScroll` |
| 渐变背景过渡 | 色相环流转（HSL H值插值） | Compose `animateColorAsState` |
| 收藏按钮 | 空心→实心填充 + 微缩放 | `animateFloatAsState` + 图标变换 |
| 导航切换 | 淡入淡出 | `Crossfade` |
| 随机一首卡片 | Y轴翻转（3D翻转） | `graphicsLayer` rotationY |
| 深色模式切换 | 全页背景渐变色过渡 1s | `animateColorAsState` |
| 文库列表→网格 | 条目重组 + 缩放动画 | `AnimatedContent` |
| 搜索防抖 | 300ms `debounce` | Kotlin Flow |
| 换一首 | 卡片翻转（正翻） | `graphicsLayer` rotationX |

---

## 9. 开发阶段建议

```
Phase 1 — 骨架搭建（1-2天）
  ├ 项目初始化：Kotlin + Compose + Material3 + Room + Retrofit
  ├ 底部导航框架（4个Tab占位）
  ├ 配色主题配置（月光白 + 墨色夜读）
  ├ 诗泉 API 接入 + 数据模型
  └ 每日页：基本布局 + 调 API 展示一首诗

Phase 2 — 核心体验（2-3天）
  ├ 每日页：上下滑动翻页动画
  ├ 每日页：哈希渐变背景生成
  ├ 每日页：智能换一首逻辑
  ├ 每日页：收藏按钮 + Room 存收藏
  └ 推荐算法：时段匹配 + 时令优先级

Phase 3 — 文库与详情（2天）
  ├ 文库页：列表模式 + 网格模式切换
  ├ 文库页：朝代标签筛选
  ├ 文库页：搜索（防抖 + API对接 + 高亮）
  └ 详情页：注释/赏析折叠面板 + 相似推荐

Phase 4 — 发现与我的（1-2天）
  ├ 发现页：热门排行（本地统计）
  ├ 发现页：随机一首翻牌动效
  ├ 发现页：本周回顾
  ├ 我的页：收藏列表 + 浏览历史
  └ 我的页：数据统计

Phase 5 — 设置与打磨（2天）
  ├ 字体切换（内置4种 + 本地ttf导入）
  ├ 字号切换
  ├ 深色模式 + 过渡动画
  ├ 每日配图开关
  ├ 画库集成（精选国风图片）
  ├ 离线缓存策略
  └ 全面动效打磨

Phase 6 — 测试与发布（1天）
  ├ 两人真机测试
  ├ 性能优化（列表流畅度/启动速度/包大小）
  ├ 生成 release APK
  └ 安装到你和对象的手机
```

---

## 10. 包结构建议

```
com.moonlight.verse/
├── MainActivity.kt
├── MoonlightApp.kt                  // Application + 主题
├── navigation/
│   ├── NavGraph.kt
│   └── BottomNavItem.kt
├── ui/
│   ├── theme/
│   │   ├── Color.kt                 // 月光白 + 墨色夜读色值
│   │   ├── Type.kt                  // 字体体系
│   │   └── Theme.kt                 // Material3 主题
│   ├── daily/
│   │   ├── DailyScreen.kt
│   │   └── PoemCard.kt              // 单首诗词卡片组件
│   ├── library/
│   │   ├── LibraryScreen.kt
│   │   ├── PoemList.kt              // 列表模式
│   │   └── PoemGrid.kt              // 网格模式
│   ├── discover/
│   │   ├── DiscoverScreen.kt
│   │   ├── HotRanking.kt
│   │   ├── RandomCard.kt            // 翻转卡片
│   │   └── WeekReview.kt
│   ├── profile/
│   │   ├── ProfileScreen.kt
│   │   ├── FavoritesScreen.kt
│   │   ├── HistoryScreen.kt
│   │   └── SettingsScreen.kt
│   ├── detail/
│   │   └── PoemDetailScreen.kt
│   └── components/
│       ├── GradientBackground.kt    // 哈希渐变生成
│       ├── FontPicker.kt
│       └── SearchBar.kt
├── data/
│   ├── local/
│   │   ├── AppDatabase.kt
│   │   ├── PoemDao.kt
│   │   └── entities/
│   ├── remote/
│   │   ├── PoetryApi.kt             // Retrofit 接口
│   │   └── PoetryApiClient.kt       // OkHttp + Retrofit 实例
│   └── repository/
│       └── PoetryRepository.kt
├── domain/
│   ├── model/
│   │   └── Poem.kt                  // 领域模型
│   └── usecase/
│       ├── DailyRecommendUseCase.kt
│       ├── SmartShuffleUseCase.kt
│       └── HashGradientUseCase.kt
└── util/
    └── HashGradientGenerator.kt     // SHA256 → HSL 算法
```

---

## 11. 不做的范围（最终确认）

- ❌ 不做任何形式的用户登录/账号/云同步
- ❌ 不做飞花令交互游戏
- ❌ 不做社区/评论/分享/私信功能
- ❌ 不做 iOS 版/平板适配（仅 Android 手机）
- ❌ 不做广告/商业化/开屏/推送
- ❌ 不用 XML 布局，100% Compose
- ❌ 不上传任何用户数据到任何服务器
- ❌ 不做音频朗读/背诵模式
- ❌ 不做诗词接龙/对战等游戏化模块

---

## 12. 附录 · 线图索引

线图初版见：`/workspace/poetry-app-wireframe.md`
审问记录见：`/workspace/projects/poetry-app/.grill/poetry-app.md`

---

*本文档替代一切口头约定，后续修改请更新此文件。*
